- **bngsim now ships a PEP 561 `py.typed` marker (issue #587).** mypy, pyright
  and IDEs now read the package's annotations and the generated
  `_bngsim_core.pyi` stub, where they previously treated bngsim as untyped.
  From @Montana.
